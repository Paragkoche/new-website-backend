import app from "@/app";
import SuperTest from "supertest";

const req = SuperTest(app);
beforeEach(() => {});
afterEach(() => {});

describe("Test api", () => {
  describe("[test 1] /user endpoint", () => {});
});
